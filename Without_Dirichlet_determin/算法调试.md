# 算法调试

## 项目结构与运行方式（你要找的那两部分）

这个仓库（`Project2/`）现在主要分三块，其中你记得的“两部分”对应：

- **强化学习 social planner（学习制度/分配规则）**
  - `Without_Dirichlet_determin/`：确定性 planner + **TD3**（主用）
  - `Dirichlet_sto/`：随机 planner（Dirichlet 策略）+ **多进程 actor-learner + V-trace**（旧/对照）
- **手工分配 baseline（按劳分配 vs 平均分配）**
  - `Without_Dirichlet_determin/Baseline.py`：`labor / equal / hybrid`

下面把“怎么跑 / 怎么设置 / 输入输出是什么”按这两块写清楚。

### A. 强化学习 social planner（TD3 确定性版，推荐从这里开始）

**目录**：`Without_Dirichlet_determin/`

**入口脚本**：`Without_Dirichlet_determin/global_trainer.py`

**运行（训练）**

- 直接跑：在项目根目录执行：`python Without_Dirichlet_determin/global_trainer.py`
- 或进入目录执行：`cd Without_Dirichlet_determin && python global_trainer.py`

**关键开关/参数在哪里改**

- 训练超参数集中在 `Without_Dirichlet_determin/global_trainer.py` 的 `TD3Config`（例如：`batch_size / actor_lr / critic_lr / gamma / tau / expl_noise / total_steps` 等）。
- 环境参数通过 `TD3Config` 里的 `env_*` 字段控制（例如：`env_R_decay / env_coop_cost / env_initial_R / env_investment_mode` 等）。
- 训练/评估切换：脚本末尾有：
  - `EVAL_ONLY = False`：训练并保存到 `Without_Dirichlet_determin/checkpoints/<run_id>/`
  - `EVAL_ONLY = True`：只评估（需要填 `EVAL_RUN_ID`，以及 `EVAL_L_LIST / EVAL_R_LIST / EVAL_EPISODE_LENGTH_LIST / EVAL_EPISODES`）

**输入 / 输出（非常重要，调试时对齐 shape）**

- 环境：`Without_Dirichlet_determin/env.py` 的 `PublicGoodsEnv`
  - `state = env.get_state()`：`(4, L, L)`，通道为：`[Stra_now, Stra_prev, P_norm, R_norm]`
  - `pi_field`（planner 的动作）：`(L, L, 5)`，每格点 5 维 `[mid, up, down, left, right]`，表示“以该格点为中心的小组公共池如何分给 5 个角色”
  - `next_state, reward, done, info = env.step(pi_field)`：
    - `next_state`：同 `state` 形状
    - `reward`：planner 的标量回报（本 TD3 默认 `env_use_cumulative_planner_reward=False`，即每步回报，不在 env 内累计）
    - `done`：是否到达 `episode_length`
    - `info`：会带一些监控量（例如 `f_C` 合作率、平均资源等；用来画曲线/早停）
- 网络：`Without_Dirichlet_determin/planner_net.py`
  - `ActorNet` 输入 `(B,4,L,L)` 输出 `(B,5,L,L)`，在 `global_trainer.select_action()` 里 softmax 后转成 `(B,L,L,5)` 给环境
  - `CriticNet` 输入 `(state, action)` 输出标量 `Q(s,a)`；训练时是双 Q（TD3 标准）

**训练产物（保存到哪里）**

- 默认保存目录：`Without_Dirichlet_determin/checkpoints/<run_id>/`
  - `actor.pt / critic1.pt / critic2.pt`（以及 target、best_* 等）
  - `training_log.csv`（训练与 eval 指标）
  - 若装了 matplotlib，会额外保存 reward/fC/loss 曲线图
- TensorBoard（需要 `cfg.save_models=True` 才会写 `events.out.tfevents*` 日志）：一般把 logdir 指到 `Without_Dirichlet_determin/checkpoints/` 即可看多次 run 的曲线
  - 启动：`tensorboard --logdir Without_Dirichlet_determin/checkpoints --port 6006`
  - 注意 `--logdir` 是“相对当前命令行所在目录”的：如果你已经 `cd Without_Dirichlet_determin`，那应该用 `tensorboard --logdir checkpoints --port 6006`
  - 如果提示 `tensorboard: 未找到命令`：先确认运行训练用的是哪个 Python/conda 环境，并在同一环境里安装/启动：
    - 安装：`python -m pip install tensorboard`
    - 启动（不依赖 PATH 里是否有可执行文件）：`python -m tensorboard.main --logdir Without_Dirichlet_determin/checkpoints --port 6006`
  - 浏览器打开：`http://localhost:6006/`
  - 如果你是在 WSL/远程机跑训练：`tensorboard --logdir Without_Dirichlet_determin/checkpoints --host 0.0.0.0 --port 6006`，再用端口转发 `ssh -L 6006:127.0.0.1:6006 <user>@<host>`

**可视化/解释策略在看什么（saliency/Grad-CAM/IG）**

- 代码在：`Without_Dirichlet_determin/viz_actor.py`
- 输出目录默认是：`Without_Dirichlet_determin/viz_outputs/`（这里只是“图片输出”，不是代码目录）
- 用法（在 `Without_Dirichlet_determin/` 下）：`python viz_actor.py --actor-path checkpoints/<run_id>/actor.pt --L 25 --r 4.0 --episode-length 150 --num-states 10 --out-dir viz_outputs --device cpu`

### B. 强化学习 social planner（Dirichlet 随机策略 + 多进程 V-trace，对照用）

**目录**：`Dirichlet_sto/`

**入口脚本**：`Dirichlet_sto/global_trainer.py`

**运行**

- `python Dirichlet_sto/global_trainer.py`

**怎么设置**

- 直接改脚本末尾 `if __name__ == "__main__":` 下面的：
  - `NUM_ACTORS / L_SIZE / R_FACTOR / DEVICE / MAX_UPDATES / EPISODE_LENGTH / T_ACTOR`
  - 保存目录 `SAVE_DIR = "checkpoints_dirichlet"`（输出到 `Dirichlet_sto/checkpoints_dirichlet/<run_id>/`）
  - 继续训练用 `LOAD_RUN_ID`

**输入 / 输出**

- 环境：`Dirichlet_sto/env.py`
  - `state`：`(3, L, L)`（这里是 3 通道版本）
  - `pi_field`：`(L, L, 5)`（同样是 5 维分配向量）
  - `env.step(pi_field)` 返回：`next_state, reward, done, info`
- 网络：`Dirichlet_sto/planner_net.py` 的 `PlannerNet`
  - 输出 `alpha (B,5,L,L)`（Dirichlet 浓度参数）和 `value (B,)`
  - Actor（`Dirichlet_sto/worker.py`）用 `Dirichlet(alpha)` 逐格点采样得到 `pi_field`
- 训练：`Dirichlet_sto/global_trainer.py` + `Dirichlet_sto/worker_utils.py`
  - 多进程 actor 往 `traj_queue` 放轨迹；learner 用 V-trace 做 off-policy 校正更新 `global_net`

### C. 手工分配 baseline（按劳分配 / 平均分配 / 混合）

**目录**：`Without_Dirichlet_determin/`

**入口脚本**：`Without_Dirichlet_determin/Baseline.py`

**运行方式 1：IDE 直接跑（推荐）**

- 打开 `Baseline.py`，保持末尾：`USE_MANUAL_CONFIG = True`
- 在 `manual_cfg = BaselineConfig(...)` 里改：
  - `allocations=("labor","equal")`（你要的“按劳+平均”就在这）
  - `L_list / r_list / episode_length / episodes`
  - 环境参数：`R_decay / coop_cost / initial_R / investment_mode` 等
- 运行：`python Without_Dirichlet_determin/Baseline.py`

**运行方式 2：命令行参数**

- 示例：`python Without_Dirichlet_determin/Baseline.py --allocation_list labor equal --L_list 25 30 --r_list 2.0 3.0 --episode_length 150 --episodes 10`
- `--allocation`：单一模式；`--allocation_list`：按列表依次跑多个模式

**输出是什么 / 保存到哪里**

- 输出目录：`Without_Dirichlet_determin/baseline_results/<时间戳+参数>/`
  - `summary.csv`：每个 `(L,r,allocation)` 的稳态合作率 `steady_mean_fC` 等指标
  - `traj_*.csv`：平均合作率 `f_C` 随时间的轨迹（可选保存）
  - `traj_*.png`：若装了 matplotlib，会画曲线

针对你的 TD3 算法在空间公共物品博弈（SPGG）中出现的“高合作后崩塌”且不收敛的问题，除了之前提到的超参数调整（Batch Size、Learning Rate），从**算法框架**和**网络结构**的深层角度来看，这里有几个非常关键的改进建议。

核心痛点分析：你的 Agent 目前可能处于“**盲目飞行**”状态——它只能看到当前的合作状态，却看不到支撑合作的“燃料”（资源 $R$）是否即将耗尽。

以下是具体的提升建议，按优先级排序：

### 1. 关键改进：在状态输入中加入“资源图层” (State Representation)

这是目前代码中最大的潜在隐患。

- **问题**：查看你的 `env.py` 中的 `get_state` 函数，输入给网络的 `state` 只包含三个通道：

  1. `Stra_now` (0/1): 当前是否合作。
  2. `Stra_prev` (0/1): 上一轮是否合作。
  3. `P_map`: 归一化后的公共池收益。

- **盲点**：Agent **看不到累计资源 $R$ 的具体数值**。它只知道 $R \ge \text{cost}$ 时是 1（合作），否则是 0。

  - 想象一下：当 $f_C=0.8$ 时，如果所有人的资源 $R=100$，系统非常稳健；如果 $R=5.1$（刚过成本线），系统即将崩塌。
  - 对于你的神经网络（尤其是 Critic），这两种状态看起来**完全一样**（输入都是 1）。因此，Critic 无法预测即将到来的崩塌，认为当前状态价值很高，直到 $R$ 耗尽瞬间崩塌，Critic 才会感到“震惊”，但为时已晚。

- **建议**：修改 `env.py`，增加第四个通道——**归一化的资源图层**。

  Python

  ```
  # 在 env.py 的 get_state 中
  # 归一化建议：可以用 R / (initial_R * 2) 或者 log(R + 1) 进行压缩
  R_norm = np.clip(self.R / 100.0, 0, 1).astype(np.float32) 
  state = np.stack([stra_now, stra_prev, P_map, R_norm], axis=0)
  ```

  同时记得修改 `global_trainer.py` 和 `planner_net.py` 中 `ActorNet/CriticNet` 的 `in_channels` 参数（从 3 改为 4）。这能让 Critic 准确判断“系统的健康度”。

### 2. 网络结构：扩大感受野 (Receptive Field)

- **问题**：目前的 `ConvBody` 只有 3 层 $3\times3$ 卷积。
  - 这意味着每个格点的输出只取决于周围 $7\times7$ 的区域。
  - 在空间博弈中，背叛者的“入侵”往往像波浪一样传播。如果感受野太小，Agent 无法感知远处传来的“背叛波”，也就无法提前做出防御性分配（例如在边界处切断背叛者的收益）。
- **建议**：
  1. **增加空洞卷积 (Dilated Convolution)**：将中间层的 `dilation` 设为 2 或 4。这可以在不增加参数量的情况下，成倍扩大感受野，让 Agent 拥有“全局视野”。
  2. **引入全局特征**：在 Critic 网络的的全连接层之前，除了卷积特征，额外拼接一些全局统计量（如当前的全局 $f_C$、全局平均 $R$）。这能帮助 Critic 区分局部震荡和全局崩塌。

### 3. 算法机制：优先经验回放 (Prioritized Experience Replay, PER)

- **问题**：你的训练日志显示崩塌是偶发的。在 `ReplayBuffer` 中，90% 的样本可能都是“高合作、高奖励”的平庸样本，而那些“从高合作开始崩塌”的**关键负样本**非常稀少。
- **后果**：在随机采样（Uniform Sampling）下，这些关键教训被淹没了，导致 Critic 总是“记吃不记打”。
- **建议**：实现 **PER**（Prioritized Experience Replay）。根据 TD-error 对样本加权，让那些预测误差大（即崩塌瞬间）的样本被更频繁地采样和训练。这能强迫模型重点学习“如何避免崩塌”。

### 4. 训练技巧：N-step Returns (N步回报)

- **问题**：资源耗尽导致的崩塌具有**滞后性**。当前的动作可能在 5-10 步之后才导致资源 $R$ 跌破阈值。TD3 默认的 1-step TD loss 传递这种延迟惩罚非常慢。
- **建议**：使用 **N-step Returns**（例如 $N=3$ 或 $N=5$）。
  - $Q(s, a) \approx r_t + \gamma r_{t+1} + \dots + \gamma^n Q(s_{t+n}, a_{t+n})$
  - 这能让 Critic 更快地将未来的崩塌惩罚关联到当前的错误动作上。

### 总结行动路线

如果不改动太大，**性价比最高**的方案是：

1. **修改输入**：立刻把 **$R$ (资源)** 加进状态通道。（这极有可能直接解决收敛后崩塌的问题）。
2. **调参**：配合之前说的大 Batch Size 和小 Learning Rate。

如果愿意改动代码结构：

3. 改网络：把中间的卷积层改成 dilation=2。
4. 改Loss：实现 N-step Return 计算 Target Q。







## 12.1结果：

![image-20251202142338292](C:\Users\79284\AppData\Roaming\Typora\typora-user-images\image-20251202142338292.png)

![image-20251202142353488](C:\Users\79284\AppData\Roaming\Typora\typora-user-images\image-20251202142353488.png)

20251203_151223参数

![alt text](1764860898546.png)

![image-20251205154727459](C:\Users\79284\AppData\Roaming\Typora\typora-user-images\image-20251205154727459.png)

20251203_133419

![1764941912022](D:\Wechat_Files\WeChat Files\wxid_3u4c52ho44un22\FileStorage\Temp\1764941912022.png)

20251205_214027

参数

![image-20251205214117995](C:\Users\79284\AppData\Roaming\Typora\typora-user-images\image-20251205214117995.png)

![image-20251205214125433](C:\Users\79284\AppData\Roaming\Typora\typora-user-images\image-20251205214125433.png)
